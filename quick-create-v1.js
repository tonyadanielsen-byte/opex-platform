(() => {
'use strict';
const DEFAULT_AREAS=['Ferdigmat','Renhold','Rekvisita'];
const clean=v=>String(v??'').trim().replace(/\s+/g,' ');
const key=v=>clean(v).toLocaleLowerCase('nb-NO');
const $=id=>document.getElementById(id);
let areas=[...DEFAULT_AREAS];

function style(){if($('opex-quick-create-style'))return;const s=document.createElement('style');s.id='opex-quick-create-style';s.textContent=`.opex-field-error{border-color:#c73513!important;box-shadow:0 0 0 3px rgba(199,53,19,.13)!important}.opex-inline-error{display:none;margin:12px 20px 0;padding:10px 12px;border-radius:9px;background:#fff0eb;border:1px solid #efc3b7;color:#9e3217;font-size:12px;font-weight:750}.opex-inline-error.show{display:block}.opex-next-hint,.opex-area-help{display:block;margin-top:5px;color:#777;font-size:11px;font-weight:500}`;document.head.appendChild(s);}
async function loadAreas(){try{const tasks=await firebase.database().ref('/tiltak').once('value');const map=new Map(DEFAULT_AREAS.map(a=>[key(a),a]));tasks.forEach(c=>{const v=clean(c.val()?.omrade);if(v)map.set(key(v),v);});areas=[...map.values()].sort((a,b)=>a.localeCompare(b,'nb'));}catch(e){console.warn('[OpEx QuickCreate] Kunne ikke laste områder',e);}}
function rememberArea(name){name=clean(name);if(!name||areas.some(a=>key(a)===key(name)))return;areas.push(name);areas.sort((a,b)=>a.localeCompare(b,'nb'));}
function areaField(){const old=$('m_omrade');if(!old||old.dataset.dynamicArea==='1')return;const input=document.createElement('input');input.id='m_omrade';input.style.width='100%';input.placeholder='Velg eller skriv område';input.autocomplete='off';input.dataset.dynamicArea='1';input.setAttribute('list','opexAreaList');const dl=document.createElement('datalist');dl.id='opexAreaList';const refresh=()=>{dl.innerHTML='';areas.forEach(a=>{const o=document.createElement('option');o.value=a;dl.appendChild(o);});};refresh();input.value=clean(old.value);old.replaceWith(input);input.insertAdjacentElement('afterend',dl);const help=document.createElement('div');help.className='opex-area-help';help.textContent='Velg eksisterende område eller skriv et nytt. Nye områder blir tilgjengelige videre.';dl.insertAdjacentElement('afterend',help);input.addEventListener('focus',refresh);input.addEventListener('input',()=>rememberArea(input.value));}
function inlineError(message,field){let box=$('opexModalInlineError');if(!box){box=document.createElement('div');box.id='opexModalInlineError';box.className='opex-inline-error';document.querySelector('#modal .modalhead')?.insertAdjacentElement('afterend',box);}document.querySelectorAll('#modal .opex-field-error').forEach(x=>x.classList.remove('opex-field-error'));if(!message){box?.classList.remove('show');return;}box.textContent=message;box.classList.add('show');if(field){field.classList.add('opex-field-error');field.scrollIntoView({block:'center',behavior:'smooth'});field.focus();}}
function makeNextOptional(){const n=$('m_nestesteg');if(!n)return;n.required=false;n.removeAttribute('required');const wrap=n.closest('.full');const label=wrap?.querySelector('label');if(label&&!label.dataset.quickHint){label.dataset.quickHint='1';const h=document.createElement('span');h.className='opex-next-hint';h.textContent='Anbefalt – kan fylles ut senere.';label.appendChild(h);}}
function saveHook(){if(typeof window.saveTask!=='function'||window.saveTask.__quickCreate)return false;const original=window.saveTask;const wrapped=function(...args){inlineError('');const isNew=!window.editKey;if(isNew){makeNextOptional();const required=[['m_tittel','Tittel'],['m_kategori','Kategori'],['m_omrade','Område'],['m_frist','Frist'],['m_eier','Eier']];for(const [id,label] of required){const f=$(id);if(f&&!clean(f.value)){inlineError(`${label} må fylles ut før tiltaket kan lagres.`,f);return;}}rememberArea($('m_omrade')?.value);}return original.apply(this,args);};wrapped.__quickCreate=true;window.saveTask=wrapped;return true;}
function modalHook(){if(typeof window.openModal!=='function'||window.openModal.__quickCreate)return false;const original=window.openModal;const wrapped=function(k,...rest){const out=original.call(this,k,...rest);setTimeout(()=>{inlineError('');areaField();if(!k)makeNextOptional();},20);return out;};wrapped.__quickCreate=true;window.openModal=wrapped;return true;}
function boot(){if(!window.firebase?.database)return setTimeout(boot,150);style();loadAreas();let n=0;const t=setInterval(()=>{n++;areaField();const a=modalHook(),b=saveHook();if((a&&b)||n>100)clearInterval(t);},150);}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();